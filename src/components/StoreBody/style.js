import styled from "styled-components";

export const ContainerStoreItem = styled.div`
    max-width: 64rem;
    margin: 0 auto;
    padding: 3.5rem 0;

    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
`;